{
  "name": "dstopic",
  "private": true,
  "version": "1.0.0",
  "description": "javascript runtime",
  "main": "index.js",
  "scripts": {
    "build": "webpack --entry ./src/index.js --mode production --target node -o dist/dstopic.js",
    "check": "prettier -c ./src/**",
    "clean": "rimraf dist",
    "format": "prettier --write ./src/**"
  },
  "devDependencies": {
    "prettier": "^1.18.2",
    "rimraf": "^2.6.3",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  },
  "dependencies": {
    "babel-core": "^7.0.0-bridge.0",
    "babel-plugin-transform-react-jsx": "^7.0.0-beta.3",
    "babel-plugin-transform-typescript": "^7.0.0-beta.3"
  }
}
